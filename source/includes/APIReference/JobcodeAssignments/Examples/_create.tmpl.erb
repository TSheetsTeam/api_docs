<% @request_body = File.read("source/includes/APIReference/JobcodeAssignments/Examples/_create-request.md.erb") -%>
<% @response_body = File.read("source/includes/APIReference/JobcodeAssignments/Examples/_create-response.md.erb") -%>

<%= partial "includes/CodeTemplates/post.md.erb",
  :locals => {
    :endpoint => "jobcode_assignments",
    :description => "Create two new jobcode assignments. One succeeds. The other fails because of a permissions issue with the `user_id`.",
    :request_body => @request_body,
    :response_body => @response_body
  }
%>
