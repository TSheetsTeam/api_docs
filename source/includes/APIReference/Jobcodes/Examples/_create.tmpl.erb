<% @request_body = File.read("source/includes/APIReference/Jobcodes/Examples/_create-request.md.erb") -%>
<% @response_body = File.read("source/includes/APIReference/Jobcodes/Examples/_create-response.md.erb") -%>

<%= partial "includes/CodeTemplates/post.md.erb",
  :locals => {
    :endpoint => "jobcodes",
    :description => "Create two new jobcodes. One succeeds. The other fails because of a duplicate `short_code` on an existing jobcode.",
    :request_body => @request_body,
    :response_body => @response_body
  }
%>
