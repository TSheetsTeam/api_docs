## Create {ENTITY_NAME}

<%= partial "includes/APIReference/{ENTITY_NAME}/Examples/create.tmpl.erb" %>

Add one or more {ENTITY_NAME} to your company.

### HTTP Request

<img src="images/post.png" alt="post"/><api>https://rest.tsheets.com/api/v1/{ENTITY_NAME}</api>

### Properties
_Pass an array of {ENTITY_NAME} objects as the value to a 'data' property (see example)._

|                |             |             |
| -------------: | :---------: | ----------- |
| **{PROP_NAME}**<br/>{required|optional} | _{DATA_TYPE}_ | {PROP_DESC} |
{ADDITIONAL_PROPS_HERE}

For a full list of properties that may be set on a {ENTITY_NAME}, see [the {ENTITY_NAME} object]({ANCHOR_LINK}).

### Status Codes

Each {ENTITY_NAME} that is created will come back with a `_status_code` and `_status_message` that will indicate whether the {ENTITY_NAME} was created successfully. If there was a problem creating a {ENTITY_NAME}, there may also be an additional field, `_status_extra`, which will contain more details about the failure.

|         |          |
| :-----: | :------- |
| <code class="{level200|level400}">{STATUS_CODE}</code> | {STATUS_DESC} |
{ADDITIONAL_CODES_AS_ABOVE}

{NOTE:REMOVE_IF_NOT_APPLICABLE}
<aside class="notice">
The maximum batch size is <i>50</i> groups. If exceeded, a <code class="standout">413: Request entity too large</code> HTTP response will be returned.
</aside>